<!DOCTYPE html>
<html>
    <head>  
        <title>Login - FTC Scouting</title>
        <link rel="icon" href="/static/favicon.png" type="image/x-icon">
        <link rel="stylesheet" href="/static/styles/global.css">
        <link rel="stylesheet" href="/static/styles/login.css">
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
        <div class="flex flex-col justify-center h-[100vh]">
            <div class="mainBox bg-white w-1/3 mx-auto rounded-md align-middle p-3">
                <p class="text-4xl font-semibold text-center w-full">Login</p>
                <form action="/api/login" class="w-[90%] mx-auto mt-2" method="post">
                    <div class="h-[2.5px] bg-gray-800 w-full mx-auto mb-2"></div>
                    {% if error %}
                    <div class="bg-red-500 p-2 rounded-md mb-4">
                        {{ error }}
                    </div>
                    {% endif %}
                    <input class="border rounded-md p-2 text-xl w-full
                    mt-4" 
                    type="text"
                    name="email"
                    placeholder="Email" />

                    <input class="border rounded-md p-2 text-xl w-full
                    mt-6" 
                    type="password"
                    name="password"
                    placeholder="Password" />

                    <div class="flex flex-row gap-x-4">
                        <input class="p-2 bg-green-500 rounded-3xl mt-6 text-2xl w-full cursor-pointer" value="Login" type="submit" />
                        <a href="/register" class="p-2 bg-blue-400 rounded-3xl mt-6 text-2xl w-full text-center cursor-pointer">Register</a>
                    </div>
                </form>
            </div>
        </div>
    </body>
</html>